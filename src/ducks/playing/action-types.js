import { NAME } from './constants';

export const ADD_CHANNEL = `${NAME}/ADD_CHANNEL`;
export const INSTRUMENT_DISPOSED = `${NAME}/INSTRUMENT_DISPOSED`;
export const PLAY_NOTE = `${NAME}/PLAY_NOTE`;
export const PREVIEW_NOTE = `${NAME}/PREVIEW_NOTE`;
export const RELEASE_ALL = `${NAME}/RELEASE_ALL`;
export const SET_CHANNELS = `${NAME}/SET_CHANNELS`;
export const UPDATE_CHANNEL = `${NAME}/UPDATE_CHANNEL`;
